# Change Summary (Local)

This file is a running, human-readable summary of notable changes made to this project.

- Scope: local reference for this workspace (kept out of Git via `.gitignore`).
- Format: append-only entries (newest at the top).

---
