# Project Rules (Local)

Keep this file short so it reliably loads into AI context.

Longer notes are split into separate files under `.trae/rules/` and `.trae/notes/`.

## Index

- Tooling: `tooling.md`
- Commands: `commands.md`
- Languages: `languages.md`

- Security: `security.md`
- AI notes workflow: `ai_notes_policy.md`

## Default Workflow

- Keep local-only docs under `local_docs/` and `.trae/` (both are gitignored).
- Record notable work in `.trae/notes/change_summary.md` after each change session.
